# Eggcrack

Java implementation

## Commands

- `./gradlew run --console plain`
- `./gradlew jlink`
- `./gradlew jlinkZip`
- `./gradlew jpackage` # needs additional packages
- `./gradlew spotlessApply`
